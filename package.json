{
  "name": "myblog",
  "version": "1.0.0",
  "description": "Flask博客应用的Netlify部署",
  "main": "index.js",
  "scripts": {
    "build": "pip install -r requirements.txt"
  },
  "dependencies": {
    "express": "^4.17.1",
    "http-proxy-middleware": "^2.0.1",
    "serverless-http": "^2.7.0"
  }
}
